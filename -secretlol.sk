on command:
    if sender is not a player:
        stop

    # začneme s názvem příkazu
    set {_full} to command

    # přidáme všechny argumenty
    loop arguments:
        set {_full} to "%{_full}% %loop-value%"

    broadcast "&7%player%: %{_full}%"
